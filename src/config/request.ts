// Define default configuration to request tool.
const TIMEOUT: number = 30000;
const METHOD: string = "GET";
const HEADERS: any = {
  "Content-type": "application/json; charset=UTF-8",
};

export { TIMEOUT, METHOD, HEADERS };
